# [학생성적프로그램]
# 1: 학생성적입력
# 2: 학생성적출력 메뉴는 항상 출력, 1을 선택하면 입력, 2는 출력

stu = []
s_no = 0
while True:
    "[학생성적프로그램]"
    print("-"*60)
    "[1.학생성적입력]"
    "[2.학생성적출력]"
    print("-"*60)
    choice = int(input("숫자를 입력하시오."))

    if choice == 1:
        print("학생성적입력")
        while True:
            no = s_no+1
            name = input("이름(입력 중단하려면, 0을 누르시오):")
            if name == "0": break
            kor = int(input("국어성적:"))
            eng = int(input("영어성적:"))
            math = int(input("수학성적:"))
            total = kor+eng+math
            avg = total/3
            stu.append([no,name,kor,eng,math,total,avg]) # 딕셔너리는 key:value로 추가해야 함
            print(name,"학생 성적이 입력되었습니다.")
            s_no += 1
    elif choice == 2:
        print("학생성적출력")
        print("번호\t이름\t국어\t영어\t수학\t합계\t평균")
        for i in stu:
            print("{}\t{}\t{}\t{}\t{}\t{}\t{}".format(*i))
        print("-"*60)
    break

# 1번은 숫자맞추기, 2번은 구구단, 3번은 2수를 입력받아 사칙연산 -> 함수로 작성
def n_game():
    pass

def gugudan():
    pass

def cal(num1,num2):
    pass

while True:
    print["3가지 게임"]
    print("-"*60)
    print("[1.숫자맞추기]")
    print("[2.구구단]")
    print("[3.사칙연산]")
    choice = int(input("숫자를 입력하시오:"))

    if choice == 1:
        import random
        r_num = random.randint(1,10)
        while True:
            print("[숫자 맞추기 게임]")
            c_num = int(input("숫자입력:"))
            if c_num == 1:
                print("정답입니다.")
                break
            if c_num > r_num:
                print("보다 작은 숫자를 넣으세요.")
            if c_num < r_num:
                print("보다 큰 숫자를 넣으세요.")

            n_game()
    if choice == 2:
        for i in range(2,10):
            for j in range(1,10):
                print(i,"X",j,"=",i*j)
                gugudan()

    if choice == 3:
        num1 = int(input("숫자입력:"))
        num2 = int(input("숫자입력:"))
        